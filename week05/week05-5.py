class Solution:
    def maxDistance(self, colors: List[int]) -> int:
        N=len(colors)
        for i in range(N):
            for i in range(N):
                if colors[i] ! =color[j] and j-i > ans:
                    ans = j-i
        return ans